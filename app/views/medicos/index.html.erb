<p id="notice"><%= notice %></p>

<h1>Medicos</h1>

<table>
  <thead>
    <tr>
      <th>Nome Completo</th>
      <th>Data de Nascimento</th>
      <th>Cpf</th>
      <th>Email</th>
      <th>Especialidade</th>
      <th>Crm</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @medicos.each do |medico| %>
      <tr>
        <td><%= medico.nomeCompleto %></td>
        <td><%= medico.dataNascimento %></td>
        <td><%= medico.cpf %></td>
        <td><%= medico.email %></td>
        <td><%= medico.especialidade %></td>
        <td><%= medico.crm %></td>
        <td><%= link_to 'Mostrar', medico %></td>
        <td><%= link_to 'Editar', edit_medico_path(medico) %></td>
        <td><%= link_to 'Deletar', medico, method: :delete, data: { confirm: 'Tem certeza que quer deltar?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Criar Medico', new_medico_path %>
